<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.Sola" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/b</item>
        <item name="colorPrimaryVariant">@color/b</item>
        <item name="colorOnPrimary">@color/f</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/b</item>
        <item name="colorSecondaryVariant">@color/b</item>
        <item name="colorOnSecondary">@color/f</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/b</item>

        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor">@color/b</item>

        <!-- Window background. -->
        <item name="android:windowBackground">@color/b</item>

        <!-- Customize your theme here. -->
    </style>
</resources>